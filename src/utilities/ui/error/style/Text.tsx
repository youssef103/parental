import styled from "styled-components";
import { colors, styling } from "../../../config";

export const Text: string = styled.span`
  color: ${colors.danger};
  font-size: ${styling.typography.fontSmallSize};
`;
